---
id: 02-01
phase: 02-3d-scene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - src/lib/grid.ts
  - src/lib/store.ts
  - src/scene/Scene.tsx
  - src/scene/Terrain.tsx
  - src/scene/Target.tsx
  - src/scene/Friendlies.tsx
  - src/scene/JTACController.tsx
  - src/components/Reticle.tsx
  - src/components/ScenarioCard.tsx
  - src/components/TalkButton.tsx
  - src/components/DebugPanel.tsx
  - src/app/page.tsx
  - scene-smoke.ts
autonomous: true
requirements:
  - SCENE-01
  - SCENE-02
  - SCENE-03
  - SCENE-04
  - SCENE-05

must_haves:
  truths:
    - "User opens / and sees a fullscreen 3D scene (Canvas element fills viewport)"
    - "Mouse-look rotates the camera (PointerLockControls engaged on click)"
    - "A red target box is visible north-east of camera; a blue billboard friendlies marker is visible to the south of the target"
    - "HUD reticle overlay shows a centered crosshair plus a 'GRID: NNNNNN' string that changes as the camera turns"
    - "Talk button (Phase 1 voice) and scenario card render on top of the scene without breaking voice round-trip"
    - "worldToGrid / gridToWorld round-trip produces the same grid string within 1m tolerance"
  artifacts:
    - path: "src/lib/grid.ts"
      provides: "worldToGrid + gridToWorld pure functions"
      exports: ["worldToGrid", "gridToWorld"]
    - path: "src/lib/store.ts"
      provides: "Zustand store for reticleGrid + transmittedGrid + impactResult"
      exports: ["useStore"]
    - path: "src/scene/Scene.tsx"
      provides: "Top-level r3f Canvas with terrain, target, friendlies, raycast useFrame"
    - path: "src/scene/Terrain.tsx"
      provides: "Flat ground plane mesh"
    - path: "src/scene/Target.tsx"
      provides: "Red box target at world [100,1,200]"
    - path: "src/scene/Friendlies.tsx"
      provides: "Blue billboard marker south of target"
    - path: "src/scene/JTACController.tsx"
      provides: "Camera position + PointerLockControls"
    - path: "src/components/Reticle.tsx"
      provides: "CSS HUD crosshair + live grid readout from store"
    - path: "src/components/ScenarioCard.tsx"
      provides: "Top-right hardcoded scenario brief"
    - path: "src/components/TalkButton.tsx"
      provides: "Extracted talk button using useRealtimeVoice"
    - path: "src/app/page.tsx"
      provides: "HUD-overlay-on-Scene composition (no logic)"
    - path: "scene-smoke.ts"
      provides: "Playwright smoke verifier for canvas presence"
  key_links:
    - from: "src/scene/JTACController.tsx (useFrame)"
      to: "src/lib/store.ts (setReticleGrid)"
      via: "raycaster -> ground plane -> worldToGrid -> store write"
      pattern: "setReticleGrid"
    - from: "src/components/Reticle.tsx"
      to: "src/lib/store.ts (reticleGrid)"
      via: "useStore selector"
      pattern: "useStore"
    - from: "src/app/page.tsx"
      to: "src/scene/Scene.tsx + src/components/{Reticle,ScenarioCard,TalkButton}.tsx"
      via: "JSX composition"
      pattern: "<Scene"
---

<objective>
Build the Phase 2 gate: a fullscreen react-three-fiber scene (terrain, target, friendlies) with PointerLockControls, a CSS HUD reticle that shows a live 6-digit faked-MGRS grid as the user mouse-looks, a hardcoded scenario card, and the Phase 1 talk button preserved as an HUD overlay.

Purpose: Establish the visual+spatial substrate that Phase 3 will hook into for `<grid>`-tag-driven bomb impact. Without this gate, voice has nowhere to land.

Output: 13 new/modified files producing a working scene at http://localhost:3000 verified by a Playwright smoke test asserting `<canvas>` presence + grid format.

**TIME BUDGET: 90 minutes total. TRIPWIRE AT 60 MINUTES** — if Task 2 has not produced a visible target with a working reticle on screen by then, STOP and execute the fallback per `.planning/intel/original-plan.md` risk register: replace 3D Scene with a 2D Canvas top-down map (terrain rect, target dot, friendlies dot, mouse-position → grid). Do NOT push past 60 min on 3D debugging.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-3d-scene/02-CONTEXT.md
@.planning/intel/original-plan.md
@.planning/phases/01-voice-scaffold/01-01-SUMMARY.md
@CLAUDE.md
@AGENTS.md
@src/app/page.tsx
@src/app/layout.tsx
@src/hooks/useRealtimeVoice.ts
@package.json
@tsconfig.json
@voice-smoke.ts

<interfaces>
<!-- Contracts the executor needs. Use these directly — no exploration required. -->

## Existing — useRealtimeVoice (DO NOT MODIFY this hook in Phase 2)
From src/hooks/useRealtimeVoice.ts:
```typescript
export type RealtimePhase = 'idle' | 'connecting' | 'listening' | 'responding' | 'error';
export interface RealtimeStatus {
  phase: RealtimePhase;
  transcript: string;
  responseText: string;
  error: string | null;
}
export function useRealtimeVoice(): {
  status: RealtimeStatus;
  analyserRef: React.RefObject<AnalyserNode | null>;
  connect: () => Promise<void>;
  disconnect: () => void;
  isConnected: boolean;
};
```
Mobile-gesture rule preserved: `connect()` MUST be invoked synchronously inside the click handler (not awaited from anything else first). The current `src/app/page.tsx` already does `void connect()` inside `onClick` — replicate that pattern in `TalkButton.tsx`.

## New contracts this plan creates

src/lib/grid.ts:
```typescript
// World coords: ground plane spans X∈[-500,500], Z∈[-500,500] (axis-aligned).
// Grid: 6-digit string, first 3 = easting (X), last 3 = northing (Z).
export function worldToGrid(x: number, z: number): string;
export function gridToWorld(grid: string): { x: number; y: number; z: number };
```

src/lib/store.ts (Zustand v5+):
```typescript
import { create } from 'zustand';
interface SceneState {
  reticleGrid: string;
  transmittedGrid: string | null;
  impactResult: unknown | null;
  setReticleGrid: (g: string) => void;
  setTransmittedGrid: (g: string | null) => void;
  setImpactResult: (r: unknown | null) => void;
}
export const useStore: import('zustand').UseBoundStore<import('zustand').StoreApi<SceneState>>;
```

src/scene/Scene.tsx (default export, 'use client'):
```typescript
export default function Scene(): JSX.Element;  // Wraps <Canvas> with terrain/target/friendlies/controller
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps + grid math + Zustand store</name>
  <read_first>
    - /Users/kalebnim/Documents/GitHub/jtac-trainer/package.json
    - /Users/kalebnim/Documents/GitHub/jtac-trainer/.planning/phases/02-3d-scene/02-CONTEXT.md (sections: Faked MGRS, Specifics)
    - /Users/kalebnim/Documents/GitHub/jtac-trainer/CLAUDE.md (Bun-only rule)
  </read_first>
  <files>
    - package.json (modified by `bun add`)
    - bun.lock (created/modified by `bun add`)
    - src/lib/grid.ts (new)
    - src/lib/store.ts (new)
  </files>
  <action>
    1. Install runtime deps with Bun ONLY (never npm/npx). Single command:
       `bun add three @react-three/fiber @react-three/drei zustand`
       Then add types as devDep:
       `bun add -d @types/three`
    2. Create `src/lib/grid.ts` with two pure functions:
       - `worldToGrid(x: number, z: number): string` — clamp x,z to [-500, 500], offset to [0, 999] via `Math.round((coord + 500) * 0.999)`, zero-pad each to 3 digits, concat as `${easting}${northing}`. Easting = X, northing = Z.
       - `gridToWorld(grid: string): { x: number; y: number; z: number }` — split into first 3 + last 3, parse to int, undo offset via `value / 0.999 - 500`, return `{ x: easting, y: 0, z: northing }`.
       - Required round-trip behaviors (must hold within 1m):
         * `worldToGrid(0, 0)` === `"500500"`
         * `worldToGrid(-500, -500)` === `"000000"`
         * `worldToGrid(500, 500)` === `"999999"`
         * `gridToWorld("500500")` returns `{x, y:0, z}` with `Math.abs(x) < 1` and `Math.abs(z) < 1`
         * `gridToWorld(worldToGrid(123, -45))` returns `{x, z}` with `Math.abs(x-123) < 1.1` and `Math.abs(z-(-45)) < 1.1`
       - No `any` types. No throws on out-of-range — clamp silently.
    3. Create `src/lib/store.ts` — Zustand v5 store, default export NOT used; named export `useStore`. Shape exactly per <interfaces> block above. `setReticleGrid` writes `reticleGrid`; `setTransmittedGrid` writes `transmittedGrid`; `setImpactResult` writes `impactResult`. Initial values: `reticleGrid: '500500'`, `transmittedGrid: null`, `impactResult: null`.
    4. Verify the round-trip with an inline node script (no test framework):
       `bun -e "import('./src/lib/grid.ts').then(m => { const cases = [[0,0,'500500'],[-500,-500,'000000'],[500,500,'999999']]; for (const [x,z,e] of cases) { const got = m.worldToGrid(x,z); if (got !== e) { console.error('FAIL', x, z, 'got', got, 'expected', e); process.exit(1); } } const rt = m.gridToWorld(m.worldToGrid(123,-45)); if (Math.abs(rt.x-123) > 1.1 || Math.abs(rt.z+45) > 1.1) { console.error('FAIL roundtrip', rt); process.exit(1); } console.log('grid OK'); })"`
  </action>
  <verify>
    <automated>
      cd /Users/kalebnim/Documents/GitHub/jtac-trainer &amp;&amp; \
      grep -q '"three"' package.json &amp;&amp; \
      grep -q '"@react-three/fiber"' package.json &amp;&amp; \
      grep -q '"@react-three/drei"' package.json &amp;&amp; \
      grep -q '"zustand"' package.json &amp;&amp; \
      test -f src/lib/grid.ts &amp;&amp; \
      test -f src/lib/store.ts &amp;&amp; \
      grep -q 'export function worldToGrid' src/lib/grid.ts &amp;&amp; \
      grep -q 'export function gridToWorld' src/lib/grid.ts &amp;&amp; \
      grep -q 'export const useStore' src/lib/store.ts &amp;&amp; \
      grep -q "from 'zustand'" src/lib/store.ts &amp;&amp; \
      bun -e "import('./src/lib/grid.ts').then(m => { const cases = [[0,0,'500500'],[-500,-500,'000000'],[500,500,'999999']]; for (const [x,z,e] of cases) { const got = m.worldToGrid(x,z); if (got !== e) { console.error('FAIL', x, z, 'got', got, 'expected', e); process.exit(1); } } const rt = m.gridToWorld(m.worldToGrid(123,-45)); if (Math.abs(rt.x-123) > 1.1 || Math.abs(rt.z+45) > 1.1) { console.error('FAIL roundtrip', rt); process.exit(1); } console.log('grid OK'); })" &amp;&amp; \
      bunx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - package.json `dependencies` contains `three`, `@react-three/fiber`, `@react-three/drei`, `zustand`
    - package.json `devDependencies` contains `@types/three`
    - `bun.lock` exists and was modified
    - `src/lib/grid.ts` exists; exports `worldToGrid` and `gridToWorld`; the inline bun -e round-trip script prints `grid OK`
    - `src/lib/store.ts` exists; named export `useStore`; imports `create` from `'zustand'`; declares `reticleGrid`, `transmittedGrid`, `impactResult` state fields plus their setters
    - `bunx tsc --noEmit` exits 0 with no new errors
  </acceptance_criteria>
  <done>
    All four packages installed via Bun; grid math passes inline round-trip script; Zustand store exposes the three fields and three setters; type-check is clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build r3f scene + reticle HUD + page composition</name>
  <read_first>
    - /Users/kalebnim/Documents/GitHub/jtac-trainer/.planning/phases/02-3d-scene/02-CONTEXT.md (sections: Camera + controls, Scene contents, Reticle + raycast, Page composition)
    - /Users/kalebnim/Documents/GitHub/jtac-trainer/src/app/page.tsx
    - /Users/kalebnim/Documents/GitHub/jtac-trainer/src/hooks/useRealtimeVoice.ts (lines 93-110 — confirm hook signature)
    - /Users/kalebnim/Documents/GitHub/jtac-trainer/AGENTS.md (Next.js 16 client-component rules)
    - /Users/kalebnim/Documents/GitHub/jtac-trainer/node_modules/next/dist/docs/01-app/index.md (only if questions about App Router client components)
  </read_first>
  <files>
    - src/scene/Scene.tsx (new)
    - src/scene/Terrain.tsx (new)
    - src/scene/Target.tsx (new)
    - src/scene/Friendlies.tsx (new)
    - src/scene/JTACController.tsx (new)
    - src/components/Reticle.tsx (new)
    - src/components/ScenarioCard.tsx (new)
    - src/components/TalkButton.tsx (new)
    - src/components/DebugPanel.tsx (new)
    - src/app/page.tsx (rewritten — composition only)
  </files>
  <action>
    Every file in `src/scene/` and `src/components/` MUST start with `'use client';` — r3f and useRealtimeVoice both require browser APIs. **Exception: `ScenarioCard.tsx` is static text and does NOT require the directive** (it's exempt from the `'use client'` grep check below; including the directive on it is allowed but not required).

    1. **`src/scene/Terrain.tsx`** — `'use client'`. Default export `Terrain`. Render:
       `<mesh rotation-x={-Math.PI/2} receiveShadow><planeGeometry args={[1000, 1000, 1, 1]} /><meshStandardMaterial color="#3b5d3a" /></mesh>`

    2. **`src/scene/Target.tsx`** — `'use client'`. Default export `Target`. Render a red box at world `[100, 1, 200]`:
       `<mesh position={[100, 1, 200]} castShadow><boxGeometry args={[3, 2, 5]} /><meshStandardMaterial color="#c0392b" /></mesh>`
       Comment above it: `// TODO Phase 5: replace with GLB tank from Poly Pizza CC0.`

    3. **`src/scene/Friendlies.tsx`** — `'use client'`. Default export `Friendlies`. Use `Billboard` from `@react-three/drei` at world `[100, 2, 1000]` containing a small blue octahedron:
       `<Billboard position={[100, 2, 1000]}><mesh><octahedronGeometry args={[2]} /><meshStandardMaterial color="#3498db" /></mesh></Billboard>`

    4. **`src/scene/JTACController.tsx`** — `'use client'`. Default export `JTACController`. Inside it:
       - `import { PointerLockControls } from '@react-three/drei';`
       - `import { useFrame, useThree } from '@react-three/fiber';`
       - `import { Raycaster, Plane, Vector3 } from 'three';`
       - `import { useStore } from '@/lib/store';`
       - `import { worldToGrid } from '@/lib/grid';`
       - On mount, set camera position to `[0, 30, 80]` and `lookAt(0, 0, 0)`. Set FOV to 60 (camera is supplied by the Canvas; assert its `.isPerspectiveCamera`, then set `.fov = 60` and `.updateProjectionMatrix()`).
       - Maintain a frame counter ref. In `useFrame`, increment counter; only run the raycast every 6 frames.
       - Raycast: build `Raycaster` whose origin is camera world position, direction is `camera.getWorldDirection(new Vector3())`. Intersect against `new Plane(new Vector3(0, 1, 0), 0)` using `ray.intersectPlane(plane, target)` where `target = new Vector3()`.
       - If `intersectPlane` returns non-null, compute `worldToGrid(target.x, target.z)` and write it via `useStore.getState().setReticleGrid(grid)` ONLY if it differs from the current `reticleGrid` (avoid unnecessary store writes).
       - Render `<PointerLockControls />`.

    5. **`src/scene/Scene.tsx`** — `'use client'`. Default export `Scene`. Compose:
       ```
       <Canvas shadows camera={{ position: [0, 30, 80], fov: 60 }} className="absolute inset-0">
         <ambientLight intensity={0.5} />
         <directionalLight position={[100, 200, 100]} intensity={1.2} castShadow />
         <Terrain />
         <Target />
         <Friendlies />
         <JTACController />
       </Canvas>
       ```
       Imports `Canvas` from `'@react-three/fiber'`. The wrapping `<div>` outside Canvas is NOT needed if Canvas itself fills via `className="absolute inset-0"` — confirm via drei/r3f docs (Canvas accepts `className`).

    6. **`src/components/Reticle.tsx`** — `'use client'`. Default export `Reticle`. Subscribe with selector:
       `const grid = useStore(s => s.reticleGrid);`
       Render an absolutely-positioned, pointer-events-none HUD:
       - Center crosshair (a `+` styled with two thin spans, ~16px arms, color `#a3ff7a`)
       - Below crosshair (offset ~28px), a `<div>` with `font-mono text-sm` text: `GRID: {grid}` in amber `#ffb84d`
       - Wrapper: `className="pointer-events-none absolute inset-0 flex items-center justify-center"`

    7. **`src/components/ScenarioCard.tsx`** — `'use client'`. Default export `ScenarioCard`. Top-right fixed card, ~280px wide, mil-spec mono on translucent dark. Hardcoded text exactly:
       `Armor column reported at grid 600200. Friendlies 800m south. No-strike zone east of the column.`
       Wrapper: `className="absolute top-4 right-4 w-[280px] rounded border border-amber-700/60 bg-black/70 p-3 font-mono text-xs text-amber-300"`. Title line: `SCENARIO BRIEF`.

    8. **`src/components/TalkButton.tsx`** — `'use client'`. Default export `TalkButton`. Move the button-and-status JSX from the current `src/app/page.tsx` into this component. Wire to `useRealtimeVoice()` (zero-arg) the same way page.tsx does today: click handler toggles connect/disconnect, `void connect()` is called synchronously inside `onClick` (DO NOT await anything else first — preserves mobile gesture window per Phase 1 fix). Position absolutely at bottom-center: `className="absolute bottom-6 left-1/2 -translate-x-1/2 z-10"`. Render the button + small phase indicator + error line.

       **REGRESSION INVARIANTS (voice-smoke.ts depends on these — MUST preserve verbatim):**
       - Button label text: literal string `Connect` when not connected/connecting, literal string `Disconnect` otherwise. (voice-smoke.ts queries `getByRole('button', { name: 'Connect' })`.)
       - Error line uses `data-testid="error"` exactly. (voice-smoke.ts queries `page.locator('[data-testid="error"]').count()`.)
       - Phase indicator uses `data-testid="phase"` exactly. (voice-smoke.ts queries `page.locator('[data-testid="phase"]').textContent()` and looks for the substring `idle`.)
       - The phase indicator's text content must include the literal word `phase:` followed by the phase string (e.g. `phase: idle`) so the existing `phaseBefore` substring assertion still matches.

    9. **`src/components/DebugPanel.tsx`** — `'use client'`. Default export `DebugPanel`. Bottom-left collapsible (use a single `useState<boolean>` with default `true`). When open, render the existing transcript/response `<pre data-testid="transcript">` / `<pre data-testid="response">` panels reading from `useRealtimeVoice()` (it's fine to call the hook again here — it's just state, not WebSocket; OR pass values via props from page if cleaner). Position: `className="absolute bottom-4 left-4 z-10 max-w-md"`. Toggle button label `[debug]`.

       NOTE: Calling `useRealtimeVoice()` in two components creates two independent hook instances. To keep the existing voice pipeline working with one instance, instead **call `useRealtimeVoice()` once in `src/app/page.tsx`** and pass the relevant slices to `TalkButton` and `DebugPanel` as props. Adjust both components to accept props:
       - `TalkButton({ status, connect, disconnect, isConnected })`
       - `DebugPanel({ transcript, responseText })`

    10. **`src/app/page.tsx`** — Rewrite as `'use client'` composition. Calls `useRealtimeVoice()` once, passes slices down. Layout:
        ```
        <main className="relative h-screen w-screen overflow-hidden bg-black font-mono text-zinc-100">
          <Scene />
          <Reticle />
          <ScenarioCard />
          <TalkButton status={status} connect={connect} disconnect={disconnect} isConnected={isConnected} />
          <DebugPanel transcript={status.transcript} responseText={status.responseText} />
        </main>
        ```
        Keep the `data-testid="phase"`, `data-testid="transcript"`, `data-testid="response"` testids alive somewhere (move them to DebugPanel and TalkButton as appropriate) — Phase 1's voice-smoke.ts depends on them.

    11. Run `bunx tsc --noEmit` and fix any errors before declaring done. Common pitfalls: `@types/three` missing → already added in Task 1; PointerLockControls in SSR → guarded by `'use client'`.
  </action>
  <verify>
    <automated>
      cd /Users/kalebnim/Documents/GitHub/jtac-trainer &amp;&amp; \
      test -f src/scene/Scene.tsx &amp;&amp; \
      test -f src/scene/Terrain.tsx &amp;&amp; \
      test -f src/scene/Target.tsx &amp;&amp; \
      test -f src/scene/Friendlies.tsx &amp;&amp; \
      test -f src/scene/JTACController.tsx &amp;&amp; \
      test -f src/components/Reticle.tsx &amp;&amp; \
      test -f src/components/ScenarioCard.tsx &amp;&amp; \
      test -f src/components/TalkButton.tsx &amp;&amp; \
      test -f src/components/DebugPanel.tsx &amp;&amp; \
      grep -l "'use client'" src/scene/Scene.tsx src/scene/Terrain.tsx src/scene/Target.tsx src/scene/Friendlies.tsx src/scene/JTACController.tsx src/components/Reticle.tsx src/components/TalkButton.tsx src/components/DebugPanel.tsx | wc -l | grep -q '^8$' &amp;&amp; \
      grep -q "from '@react-three/fiber'" src/scene/Scene.tsx &amp;&amp; \
      grep -q "Canvas" src/scene/Scene.tsx &amp;&amp; \
      grep -q "PointerLockControls" src/scene/JTACController.tsx &amp;&amp; \
      grep -q "useFrame" src/scene/JTACController.tsx &amp;&amp; \
      grep -q "Raycaster" src/scene/JTACController.tsx &amp;&amp; \
      grep -q "intersectPlane" src/scene/JTACController.tsx &amp;&amp; \
      grep -q "setReticleGrid" src/scene/JTACController.tsx &amp;&amp; \
      grep -q "Billboard" src/scene/Friendlies.tsx &amp;&amp; \
      grep -q "useStore" src/components/Reticle.tsx &amp;&amp; \
      grep -q "GRID:" src/components/Reticle.tsx &amp;&amp; \
      grep -q "Armor column reported at grid 600200" src/components/ScenarioCard.tsx &amp;&amp; \
      grep -q "useRealtimeVoice" src/app/page.tsx &amp;&amp; \
      grep -q "&lt;Scene" src/app/page.tsx &amp;&amp; \
      grep -q "&lt;Reticle" src/app/page.tsx &amp;&amp; \
      grep -q "&lt;TalkButton" src/app/page.tsx &amp;&amp; \
      grep -q "&gt;Connect&lt;" src/components/TalkButton.tsx &amp;&amp; \
      grep -q "&gt;Disconnect&lt;" src/components/TalkButton.tsx &amp;&amp; \
      grep -q 'data-testid="error"' src/components/TalkButton.tsx &amp;&amp; \
      grep -q 'data-testid="phase"' src/components/TalkButton.tsx &amp;&amp; \
      bunx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - All 9 new component files exist under `src/scene/` and `src/components/`
    - Every file in src/scene/ and the four new files in src/components/ begin with `'use client';` (8 files total verified by grep -l)
    - `src/scene/Scene.tsx` imports from `@react-three/fiber` and renders `<Canvas>`
    - `src/scene/JTACController.tsx` references all of: `PointerLockControls`, `useFrame`, `Raycaster`, `intersectPlane`, `setReticleGrid` (proves raycast → store write wiring)
    - `src/scene/Friendlies.tsx` references `Billboard`
    - `src/components/Reticle.tsx` calls `useStore` and renders the literal string `GRID:`
    - `src/components/ScenarioCard.tsx` contains the literal hardcoded scenario text `Armor column reported at grid 600200`
    - `src/app/page.tsx` invokes `useRealtimeVoice` exactly once and renders `<Scene`, `<Reticle`, `<TalkButton` JSX tags
    - `src/components/TalkButton.tsx` contains the literal text `>Connect<` AND `>Disconnect<` (preserves voice-smoke.ts button label query)
    - `src/components/TalkButton.tsx` contains `data-testid="error"` AND `data-testid="phase"` (preserves voice-smoke.ts error/phase locators)
    - `bunx tsc --noEmit` exits 0 with no new errors
    - Dev server (already running as background task `bp4sru456`) does not log fatal errors after this change — executor SHOULD curl http://localhost:3000 once to confirm 200 status (`curl -s -o /dev/null -w '%{http_code}' http://localhost:3000` returns `200`)
  </acceptance_criteria>
  <done>
    Scene + HUD components exist, page composition wired, type-check clean, dev server returns 200 on `/`. Tripwire check: if at the 60-min mark from phase start the executor cannot point a browser at localhost:3000 and see a target with a working reticle, STOP and invoke the 2D-Canvas fallback per the Risk Register (return PHASE FALLBACK to orchestrator instead of completing this task).
  </done>
</task>

<task type="auto">
  <name>Task 3: Playwright scene smoke test</name>
  <read_first>
    - /Users/kalebnim/Documents/GitHub/jtac-trainer/voice-smoke.ts
    - /Users/kalebnim/Documents/GitHub/jtac-trainer/CLAUDE.md (Playwright CLI/SDK only — no MCP server)
  </read_first>
  <files>
    - scene-smoke.ts (new — repo root, mirrors voice-smoke.ts pattern)
  </files>
  <action>
    Create `scene-smoke.ts` at repo root using the same `import { chromium } from 'playwright';` pattern as `voice-smoke.ts`. The script:
    1. Launches headless Chromium (no fake-media flags needed for scene smoke; r3f does not need mic)
    2. Grants microphone permission anyway (so TalkButton doesn't error on initialization that calls `useRealtimeVoice`)
    3. Navigates to `http://localhost:3000`
    4. Waits up to 10s for `canvas` selector
    5. Asserts:
       - `page.locator('canvas').count()` returns 1 (the r3f Canvas)
       - `page.locator('text=GRID:').count()` returns ≥ 1 (Reticle renders)
       - `page.locator('text=Armor column reported at grid 600200').count()` returns 1 (ScenarioCard renders)
       - The Reticle text matches regex `GRID: \d{6}` (live grid format)
    6. Captures and logs any `console.error` events; fails the script (exit 1) if any fatal error message contains `Error` or `Cannot`
    7. On success, prints `scene-smoke OK`. On failure, prints `FAIL: <reason>` and `process.exit(1)`.
    8. Closes browser at the end (in finally).

    Run pattern (do NOT start a dev server in the script — the orchestrator already has `bun dev` running as background task `bp4sru456`):
    `bun scene-smoke.ts`
  </action>
  <verify>
    <automated>
      cd /Users/kalebnim/Documents/GitHub/jtac-trainer &amp;&amp; \
      test -f scene-smoke.ts &amp;&amp; \
      grep -q "from 'playwright'" scene-smoke.ts &amp;&amp; \
      grep -q "localhost:3000" scene-smoke.ts &amp;&amp; \
      grep -q "canvas" scene-smoke.ts &amp;&amp; \
      grep -q "GRID:" scene-smoke.ts &amp;&amp; \
      grep -q "Armor column reported at grid 600200" scene-smoke.ts &amp;&amp; \
      timeout 60 bun scene-smoke.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `scene-smoke.ts` exists at repo root
    - Imports `chromium` from `'playwright'` (NOT from any MCP source)
    - References `localhost:3000`, `canvas`, `GRID:`, and the literal scenario string
    - Running `bun scene-smoke.ts` against the already-running dev server prints `scene-smoke OK` and exits 0 within 60s
    - No fatal page errors in the captured console output
  </acceptance_criteria>
  <done>
    Smoke script exists, runs green against localhost:3000, confirms canvas + reticle + scenario card all render. This is the Phase 2 gate.
  </done>
</task>

</tasks>

<verification>
End-of-phase checks (run after all 3 tasks complete):

1. `bunx tsc --noEmit` exits 0
2. `bun scene-smoke.ts` exits 0 with `scene-smoke OK`
3. Manual sanity (optional, executor judgment): visit http://localhost:3000 in actual Chrome, click in the canvas to engage pointer-lock, move the mouse, confirm the GRID: NNNNNN string visibly changes
4. Voice did not regress: `bun voice-smoke.ts` still passes (the Phase 1 testids are still present in DebugPanel/TalkButton)

If verification 1 or 2 fail → fix before declaring phase complete.
If verification 4 fails → the page.tsx refactor broke voice; fix before declaring phase complete.
</verification>

<success_criteria>
- All 5 SCENE-XX requirements satisfied:
  - SCENE-01: Scene.tsx renders Canvas with Terrain + Target + Friendlies (Billboard)
  - SCENE-02: JTACController fixes camera at [0, 30, 80] and uses PointerLockControls
  - SCENE-03: Reticle.tsx renders crosshair + `GRID: NNNNNN` updated live from store
  - SCENE-04: grid.ts round-trips cleanly (proven by inline bun -e script in Task 1) AND raycast in JTACController feeds it (proven by `intersectPlane` + `setReticleGrid` greps in Task 2)
  - SCENE-05: ScenarioCard.tsx (top-right) + TalkButton.tsx (bottom-center) both overlay the scene
- `bunx tsc --noEmit` clean
- `bun scene-smoke.ts` green
- Voice round-trip not regressed (`bun voice-smoke.ts` still green)
- 90-min budget respected; if 60-min tripwire hit before Task 2's gate (target + reticle on screen), executor invoked the 2D-Canvas fallback rather than pushing through
</success_criteria>

<output>
After completion, create `.planning/phases/02-3d-scene/02-01-SUMMARY.md` documenting:
- Files created (with line counts)
- Whether the 60-min tripwire was hit and which path was taken (3D as planned, or 2D fallback)
- Any deviations from this plan and root-cause justification (per Phase 1 SUMMARY style)
- Verification commands run and their results
- Any voice regression encountered during page.tsx refactor and how it was resolved
</output>
